import type { ServiceSlug } from "@/lib/types";

// Placeholder — icon set lands with the branding pass.
export default function ServiceIcon({ slug }: { slug: ServiceSlug }) {
  void slug;
  return null;
}
