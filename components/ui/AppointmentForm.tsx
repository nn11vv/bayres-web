import type { Locale } from "@/lib/types";

// Placeholder — wired to /api/citas in the appointment-form prompt.
export default function AppointmentForm({ locale }: { locale: Locale }) {
  void locale;
  return null;
}
