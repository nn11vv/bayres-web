import type { Locale } from "@/lib/types";

// Placeholder — JSON-LD LocalBusiness schema lands in the SEO pass.
export default function SchemaLocalBusiness({ locale }: { locale: Locale }) {
  void locale;
  return null;
}
