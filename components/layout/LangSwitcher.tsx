"use client";

import type { Locale } from "@/lib/types";

// Placeholder — real markup/styling lands with the layout pass.
export default function LangSwitcher({ locale }: { locale: Locale }) {
  void locale;
  return null;
}
