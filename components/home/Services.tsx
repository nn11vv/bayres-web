import type { Locale } from "@/lib/types";

// Placeholder — filled from content/[locale]/services.ts in the content pass.
export default function Services({ locale }: { locale: Locale }) {
  void locale;
  return null;
}
