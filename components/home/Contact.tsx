import type { Locale } from "@/lib/types";

// Placeholder — wraps AppointmentForm once the form prompt lands.
export default function Contact({ locale }: { locale: Locale }) {
  void locale;
  return null;
}
