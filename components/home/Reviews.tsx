import type { Locale } from "@/lib/types";

// Placeholder — filled from content/[locale]/reviews.ts in the content pass.
export default function Reviews({ locale }: { locale: Locale }) {
  void locale;
  return null;
}
