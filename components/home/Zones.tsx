import type { Locale } from "@/lib/types";

// Placeholder — filled from content/[locale]/zones.ts in the content pass.
export default function Zones({ locale }: { locale: Locale }) {
  void locale;
  return null;
}
