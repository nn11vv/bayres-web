import type { Locale } from "@/lib/types";

// Placeholder — filled from content/[locale]/home.ts in the content pass.
export default function Hero({ locale }: { locale: Locale }) {
  void locale;
  return null;
}
